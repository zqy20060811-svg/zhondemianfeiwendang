# 本地开发环境配置
# 复制此文件为 application-local.yml 并填入你的真实配置
# 启动时添加参数：--spring.profiles.active=local

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zhongdemianfeiwendang?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 1234

  data:
    redis:
      host: localhost
      port: 6379
      database: 0

jwt:
  secret: zhongdemianfeiwendang-jwt-secret-key-2024
